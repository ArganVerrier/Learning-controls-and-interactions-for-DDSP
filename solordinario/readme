Ce notebook permet d'extraire 3 secondes de chaque fichier de Solodinario puis créer un dictionnaire "dict_env" :
  - dict_env['env'] => des array numpy avec 2 lignes : une pour l'enveloppe de velocité et une pour l'enveloppe de pitch
  - dict_env['file_name'] => nom de chaque fichier
  - dict_env['vel'] => velocite générale (ff, mm ou pp)
  - dict_env['pitch'] => pitch générale (B4 ou A#3 par exemple)
  - dict_env['corde'] => corde jouée

Le dictionnaire est enregistré dans le fichier "data_dict.npy" donc pas besoin de faire tourner tout le notebook, on peut simplement le load avec la commande :
  dict_r = np.load('data_dict.npy', allow_pickle=True).item()

** Solordinario_PrepareDataSet.ipynb :
Prépare le dataSet à partir du dictionnaire

format : env. de loudness (279*750), env. de pitch (279*750), vélocité (279*1), note (279*1)

vélocité : pp=1., mf=2., ff=3 (et donc 0 devrait être une absence de note)
note : note convertie au format midi

** midi_numbers.py :
convertie des notes au format midi (entre autres), le lien vers la source est dans le fichier

Je n'ai pas encore touché le VAE, mais dans l'idée:
- partie encodeur : entrainé à retrouver le couple (vélocité, note) à partir des enveloppes de loudness et de pitch.
- partie décodeur : entrainé à retrouver les enveloppes de loudness et de pitch à partir du couple (vélocité, note).
